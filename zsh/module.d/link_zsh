# vim: ft=zsh
function link_zsh() {
  reply=(
    "$DOTFILE/zshrc" "$HOME/.zshrc"
    "$DOTFILE/zprofile" "$HOME/.zprofile"
  )

  if [ -d "$NAVI_PATH" ]; then
    reply+=(
      "$DOTFILE/zsh/cheats/git.cheat" "$NAVI_PATH/git.cheat"
    )
  fi
}
